projection;
strict ( 2 );
use draft;

define behavior for YDMO_PARTNER_C alias PartnerBDC
{
  use create;
  use update;
  use delete;

  use action fillEmptyStreets;
  use action clearAllEmptyStreets;
  use action copyLine;
  use action withpopup;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
  use action Resume;

}